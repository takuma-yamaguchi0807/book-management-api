## Coding Policy for Agents

このプロジェクトでは、エージェントは以下の方針に沿ってコードを生成すること。

### 1. 設計思想

- 基本はシンプルなレイヤード構成とし、必要に応じてドメインロジックをドメイン層へ集約する（軽量な DDD 的アプローチ）。
- controller / service / repository の責務を明確に分離し、依存方向を乱さないこと。

### 2. 情報源と判断基準

- Spring Boot / Kotlin / jOOQ の **公式ドキュメント** を最優先とする。
- 設計や実装の判断はできる限り **ベストプラクティス** に沿うこと。
- 過剰な抽象化や複雑化は避け、技術課題に求められる適度な設計に留める。
- プロジェクトの仕様については `docs` 配下のドキュメントを参照すること。

### 3. コード作成方針

- 指定された技術スタック（Kotlin / Spring Boot / jOOQ）を正しく適用し、不要な技術を持ち込まない。
- ビルド可能・実行可能な状態を常に維持する。
- 仕様に沿った正しい動作を優先し、特にドメインルールは service（または domain）層で明確に扱う。
- クラス名・関数名・変数名は役割が一目で分かるように付ける。
- Kotlin の null 安全性を尊重し、`!!` を避けて nullable / non-nullable を適切に扱う。
- 再代入を避け `val` を基本とし、Kotlin の一般的な書き方に従う。
- コードフォーマットや構成を統一し、読みやすさと一貫性を重視する。

### 4. 過剰設計の回避

- インターフェース乱立、無駄な抽象化、複雑なパターンの導入は避ける。
- 技術課題に不要な DDD 過剰設計（Aggregate 分割・Event など）は入れない。

### 5. テスト方針

- 可能な範囲で単体テストを作成する。
- 特にビジネスルール（価格・出版ステータス・著者数など）はテストしやすく実装する。
- テストは JUnit など標準的な構成に従い、正しい仕様を確認することを優先する。

### 6. コードレビュー方針

コードレビューを行う際は、以下の観点から評価し、**すべて日本語で回答**すること。

#### レビュー観点

1. **DDD（ドメイン駆動設計）への準拠**

   - ドメインロジックが適切にドメイン層に集約されているか
   - 値オブジェクトやエンティティが適切に設計されているか
   - 集約ルートの設計が適切か
   - ドメイン層とインフラ層の依存関係が正しいか（ドメイン層がインフラ層に依存していないか）

2. **Kotlin らしい書き方**

   - Kotlin のイディオムを適切に使用しているか（data class、sealed class、拡張関数など）
   - null 安全性が適切に扱われているか（`!!` の使用を避けているか）
   - 再代入を避け `val` を基本としているか
   - コレクション操作（map、filter、groupBy など）を適切に使用しているか
   - スコープ関数（let、apply、run など）を適切に使用しているか

3. **REST API の準拠**

   - HTTP メソッドが適切に使用されているか（GET、POST、PUT など）
   - HTTP ステータスコードが適切か（200、201、204、400、404、422、500 など）
   - リソースの設計が RESTful か（リソース指向の URL 設計）
   - エラーレスポンスの形式が適切か

4. **ライブラリのベストプラクティス・デファクトスタンダード**

   - Spring Boot のベストプラクティスに沿っているか（依存性注入、トランザクション管理など）
   - jOOQ の適切な使用方法か（型安全なクエリ構築、N+1 問題の回避など）
   - Flyway の適切な使用方法か（マイグレーションスクリプトの命名規則など）
   - Kotlin 標準ライブラリの適切な使用か

5. **可読性・保守性**

   - クラス名・関数名・変数名が役割を明確に表しているか
   - コードの構造が理解しやすいか（適切な分割、適度な抽象化）
   - コメントが適切に記述されているか（過不足なく、意図が明確か）
   - マジックナンバーや文字列リテラルが適切に定数化されているか

6. **過剰設計の回避**
   - 不要な抽象化やインターフェースが存在しないか
   - 技術課題に対して適度な設計になっているか
   - 将来の拡張性を過度に考慮していないか
   - シンプルで理解しやすい設計になっているか

#### レビュー時の注意事項

- レビューは**すべて日本語で回答**すること
- 指摘事項がある場合は、具体的な改善案を提示すること
- 良い点も含めて、バランスの取れたレビューを心がけること
- プロジェクトの方針やドキュメント（AGENTS.md、docs/ 配下）に沿っているかも確認すること

---

上記方針に従い、仕様に沿ったシンプルで保守しやすいコードを生成すること。
