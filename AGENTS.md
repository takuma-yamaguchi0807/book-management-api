## Coding Policy for Agents

このプロジェクトでは、エージェントは以下の方針に沿ってコードを生成すること。

### 1. 設計思想

- 基本はシンプルなレイヤード構成とし、必要に応じてドメインロジックをドメイン層へ集約する（軽量な DDD 的アプローチ）。
- controller / service / repository の責務を明確に分離し、依存方向を乱さないこと。

### 2. 情報源と判断基準

- Spring Boot / Kotlin / jOOQ の **公式ドキュメント** を最優先とする。
- 設計や実装の判断はできる限り **ベストプラクティス** に沿うこと。
- 過剰な抽象化や複雑化は避け、技術課題に求められる適度な設計に留める。
- プロジェクトの仕様については `docs` 配下のドキュメントを参照すること。

### 3. コード作成方針

- 指定された技術スタック（Kotlin / Spring Boot / jOOQ）を正しく適用し、不要な技術を持ち込まない。
- ビルド可能・実行可能な状態を常に維持する。
- 仕様に沿った正しい動作を優先し、特にドメインルールは service（または domain）層で明確に扱う。
- クラス名・関数名・変数名は役割が一目で分かるように付ける。
- Kotlin の null 安全性を尊重し、`!!` を避けて nullable / non-nullable を適切に扱う。
- 再代入を避け `val` を基本とし、Kotlin の一般的な書き方に従う。
- コードフォーマットや構成を統一し、読みやすさと一貫性を重視する。

### 4. 過剰設計の回避

- インターフェース乱立、無駄な抽象化、複雑なパターンの導入は避ける。
- 技術課題に不要な DDD 過剰設計（Aggregate 分割・Event など）は入れない。

### 5. テスト方針

- 可能な範囲で単体テストを作成する。
- 特にビジネスルール（価格・出版ステータス・著者数など）はテストしやすく実装する。
- テストは JUnit など標準的な構成に従い、正しい仕様を確認することを優先する。

---

上記方針に従い、仕様に沿ったシンプルで保守しやすいコードを生成すること。
